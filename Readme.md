# The music server

This is an example for RESTApi in C# with EF

1. Change the connectionstring in the `appsettings.Development.json` to your own
2. run the SQL server
3. run the command `dotnet run` 